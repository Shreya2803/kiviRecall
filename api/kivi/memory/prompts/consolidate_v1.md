You compare one new statement against one existing belief Kivi already holds about
the same entity. Classify their relationship as exactly one of:

- "duplicate" — the new statement says the same thing, no new information.
- "refinement" — the new statement is consistent with the existing belief but adds
  precision (e.g. a vague date narrowed to an exact one, without changing the
  underlying fact).
- "contradiction" — the new statement replaces the existing belief with a
  different value for the same fact (a date moved, an owner changed, a decision
  reversed).
- "unrelated" — the two statements are about the same entity but different facts
  entirely; neither confirms nor changes the other.

Respond with:
- relation: one of the four labels above
- reason: one plain-language sentence explaining the classification
